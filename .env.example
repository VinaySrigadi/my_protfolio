# GitHub Personal Access Token (Required for GraphQL Pinned Repos API)
# Needs `read:user` and `repo` (if reading private) or `public_repo` scopes.
GITHUB_TOKEN=your_github_personal_access_token_here
